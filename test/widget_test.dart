import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AgroLedger smoke test', (WidgetTester tester) async {
    // Базовый тест для проверки успешной компиляции проекта.
    expect(true, true);
  });
}
