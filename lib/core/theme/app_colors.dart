import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  AppColors._();

  // Premium Sage Palette
  static const Color sagePrimary = Color(0xFF4A6B53);
  static const Color sageLight = Color(0xFF87A987);
  static const Color sageDark = Color(0xFF2C3E31);

  // Warm Cream Backgrounds
  static const Color creamBackground = Color(0xFFF9F6EE);
  static const Color creamSurface = Color(0xFFFFFFFF);

  // Typography Colors
  static const Color textDark = Color(0xFF2D3436);
  static const Color textLight = Color(0xFF7F8C8D);

  // Accents & Functional Colors
  static const Color accentGold = Color(0xFFD4AF37);
  static const Color errorSoft = Color(0xFFE74C3C);
  
  // Shadows & Overlays
  static const Color shadowColor = Color(0xFF000000);
}
